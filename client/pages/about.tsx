import React from "react";
import Layout from "../components/Layout";

export default function AboutPage() {
  return (
    <Layout>
      <div id="About">
        SWR(React) × kaminari(Ruby on Rails)でページネーション(キャッシュによるパフォーマンス最適化付き)を実装してみる！
      </div>
    </Layout>
  );
};
